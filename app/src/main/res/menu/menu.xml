<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:title="@string/menu_camara"
        android:id="@+id/camera" app:showAsAction="never"
        android:visible="true" />
    <item android:id="@+id/contacts"
        app:showAsAction="never"
        android:title="@string/menu_contactos"
        android:visible="true" />
    <item android:id="@+id/location"
        app:showAsAction="never"
        android:title="@string/menu_localizacion"
        android:visible="true"
        android:enabled="true" />
</menu>
